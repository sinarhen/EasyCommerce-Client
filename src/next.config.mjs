/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        remotePatterns: [
            {
                protocol: 'https',
                hostname: 'images.unsplash.com',// Delete this line after testing
            }
        ]
    }
};

export default nextConfig;
